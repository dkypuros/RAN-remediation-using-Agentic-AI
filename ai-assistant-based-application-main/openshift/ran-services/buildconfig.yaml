apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: ran-services
  labels:
    app: ran-services
spec:
  output:
    to:
      kind: ImageStreamTag
      name: ran-services:latest
  source:
    type: Binary
    binary: {}
  strategy:
    type: Docker
    dockerStrategy:
      dockerfilePath: Dockerfile
  triggers:
  - type: ConfigChange
  - type: ImageChange
