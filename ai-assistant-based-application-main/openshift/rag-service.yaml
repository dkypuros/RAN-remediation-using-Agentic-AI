apiVersion: v1
kind: Service
metadata:
  name: rag-service
  namespace: ai-assistant
spec:
  selector:
    app: rag-service
  ports:
  - port: 50052
    targetPort: 50052
    protocol: TCP
  type: ClusterIP
