# Placeholder OpenShift Secret for vLLM API app
apiVersion: v1
kind: Secret
metadata:
  name: vllm-app-secret
  labels:
    app: vllm-app
type: Opaque
data:
  # Example: API_KEY: <base64-encoded-value>
  # Add secrets here as needed
